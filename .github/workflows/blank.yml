name: CI Only (No Docker)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: CI pass check
        run: |
          echo "CI succeeded ✅"
          echo "Branch: ${GITHUB_REF}"
          echo "Commit: ${GITHUB_SHA}"
          ls -al
